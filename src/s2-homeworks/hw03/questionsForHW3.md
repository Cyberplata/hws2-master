    HW3
1. Для setUsers нужно указать правильный тип. В данном случае, это функция, которая принимает новый массив UserType[] и не возвращает ничего (то есть void).
   ```export type SetUsersType = (users: UserType[]) => void;
2. Зачем передавать setUsers в pureAddUserCallback? `Функция setUsers — это функция, возвращаемая хуком useState, которая позволяет обновить состояние пользователей (users). Поскольку pureAddUserCallback является чистой функцией, она не имеет доступа к состоянию напрямую и не может сама вызвать setUsers. Поэтому setUsers передается в качестве аргумента, чтобы pureAddUserCallback могла обновить состояние.`
3. Так вот мне нужно протипизировать было pureAddUser, а именно addUserCallback и я чтобы не дублировать код (name: string) => void в параметрах создал addUserCallbackType. Правильно ли это или нужно было дублировать эту типизацию (name: string) => void.
   Либо есть вариант достать определенное свойство из GreetingContainerPropsType.addUserCallback я так пробывал - вроде бы нельзя же.
   Как правильно делать? `Заключение
   Использование существующих типов напрямую делает код более прозрачным и уменьшает вероятность ошибок. Однако, если функция или тип используется часто и имеет сложную структуру, имеет смысл определить его отдельно. В твоем случае, типизация функций напрямую делает код более понятным и проще в сопровождении. Вместо создания addUserCallbackType, используй (name: string) => void напрямую, так как это более понятно и не требует лишних определений.`